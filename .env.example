# Database Configuration
DATABASE_URL="file:./prisma/smokeping.db"

# Monitoring Settings
PING_INTERVAL=60000     # Ping interval in milliseconds (default: 60 seconds)
PING_COUNT=10           # Number of ping packets per test (default: 10)
PING_TIMEOUT=2000       # Ping timeout in milliseconds (default: 2 seconds)

# Application Settings
NEXT_PUBLIC_APP_NAME="Better Smokeping"
NODE_ENV=production

# Next.js Settings
PORT=3000
HOSTNAME="0.0.0.0"
